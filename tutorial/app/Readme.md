ImageKit is a cloud-based image and video management + optimization + CDN delivery service.
image kit handle the uploading and managing the images uploaded by the user

basically in our api the uer will send the photo via api the image will be uploaded to imagekit and url will be stored and with the help of url the image will be retrieved.